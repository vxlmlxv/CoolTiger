rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /conversations/{allPaths=**} {
      allow write: if true;      // app uploads audio
      allow read: if request.auth != null; // tighten later with signed URLs
    }
  }
}